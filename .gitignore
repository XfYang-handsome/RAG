# ============ Python ============
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/

# ============ Runtime data (local DB / uploads / logs / task state) ============
milvus/
redis/
/data/
models/

# ============ eval 运行时产物（评测集 dataset.json 保留入库） ============
eval/data/runs/
eval/data/scores/
eval/data/report.md

# ============ Frontend ============
frontend/node_modules/
static/dist/

# ============ Sensitive config (contains real api_key / DB password) ============
config/models.json
config/db.json

# ============ IDE / tools ============
.vscode/
.idea/
.playwright-cli/

# ============ OS ============
.DS_Store
Thumbs.db
*.log
